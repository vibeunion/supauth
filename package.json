{
  "name": "supaoauth",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/sdks/*"
  ],
  "scripts": {
    "setup": "bun run scripts/setup.ts",
    "migrate": "bun run --filter '@supauth/auth-server' migrate",
    "dev": "bun run scripts/dev.ts",
    "dev:admin": "bun run --filter '@supauth/admin-console' dev",
    "dev:server": "bun run --filter '@supauth/auth-server' dev",
    "build": "bun run --filter '@supauth/shared' build && bun run --filter '@supauth/sdk-typescript' build && bun run --filter '@supauth/sdk-auth-ui' build && bun run --filter '@supauth/auth-server' build && bun run --filter '@supauth/admin-console' build",
    "typecheck": "bun run --filter '@supauth/shared' build && bun run --filter '@supauth/sdk-typescript' build && bun run --filter '@supauth/sdk-auth-ui' build && bun run --filter '@supauth/auth-server' typecheck",
    "test": "bun run --filter '@supauth/shared' test && bun run --filter '@supauth/sdk-typescript' test && bun run --filter '@supauth/sdk-auth-ui' test && bun run --filter '@supauth/auth-server' test",
    "check": "bun run typecheck && bun run test && bun run --filter '@supauth/admin-console' build",
    "release:gate": "bun run scripts/release-gate.ts",
    "backup:drill": "bun run scripts/backup-restore-drill.ts backup",
    "capacity:baseline": "bun run scripts/capacity-baseline.ts"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.106.1",
    "@types/bun": "^1.2.0",
    "typescript": "^5.8.0"
  }
}
