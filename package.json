{
  "name": "supaoauth",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/sdks/*"
  ],
  "scripts": {
    "setup": "bun run scripts/setup.ts",
    "migrate": "bun run --filter '@supaoauth/auth-server' migrate",
    "dev": "bun run scripts/dev.ts",
    "dev:admin": "bun run --filter '@supaoauth/admin-console' dev",
    "dev:server": "bun run --filter '@supaoauth/auth-server' dev",
    "build": "bun run --filter '@supaoauth/shared' typecheck && bun run --filter '@supaoauth/auth-server' build && bun run --filter '@supaoauth/admin-console' build",
    "typecheck": "bun run --filter '@supaoauth/shared' typecheck && bun run --filter '@supaoauth/auth-server' typecheck",
    "test": "bun run --filter '@supaoauth/shared' test && bun run --filter '@supaoauth/auth-server' test",
    "check": "bun run typecheck && bun run test && bun run --filter '@supaoauth/admin-console' build"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.8.0"
  }
}
